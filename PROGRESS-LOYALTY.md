# PROGRESS — Loyalty Engine hợp nhất "quản trị sâu" (coalition, đa công ty)

> Duy trì ngữ cảnh qua phiên. Sau `/clear`: đọc file này + plan `~/.claude/plans/hi-n-ph-n-qu-n-l-logical-raven.md` + research `…-agent-aa2a7d6842910ff9f.md` + `CLAUDE.md` TRƯỚC.
> Branch `feat/foundation-core` · DB `AI_CDP_Pro` PG18 @5433 · demo 192.168.1.93:8070 (admin/Och@2026).

## Quyết định chốt
- Đa công ty, hạch toán riêng + **cơ chế chuyển đổi điểm** (brand → điểm CHUNG tập đoàn coalition).
- **Nhiều loại điểm/ví theo brand** + điểm nhóm + tỷ giá quy đổi.
- Làm **TRỌN #10** (ledger đa-currency, tier, expiry/breakage, reward/voucher, redemption cross-brand,
  campaign+gamification, inter-company settlement + liability IFRS15/ASC606, POS realtime, thẻ/QR, stored-value, admin FE, RBAC).
- Tính **quản trị + mở rộng 10/10** (config append-only + audit, rule engine, data-driven, RBAC theo pháp nhân).

## Nguyên tắc GIỮ NGUYÊN (không phá)
Double-entry + balance=projection (cấm âm) nay **per (txn,currency)**; idempotency+fingerprint; advisory-lock per occ;
reservation state machine; ledger append-only + audit; earn KHÔNG ở ingest (qua rule/journey); consent chỉ gate ở activation;
tương thích ngược chữ ký `earn/reserve/capture/release/getBalance` (mặc định = GROUP currency).

## Lộ trình (L0→L9) — mỗi phase: TDD → verify → /review → /codex → commit
- [x] **L0 — Nền đa-scope HOÀN TẤT** (migration `025_loyalty_company_currency.sql` + kernel currency-aware + fix merge đa-ví). **592 test PASS**, tsc sạch, zero regression.
  - `cdp.company` (occ_fnb, occ_hotel) + `cdp.brand.company_id` (map F&B vs hotel).
  - `cdp.point_currency` (GROUP 'OCC_POINT' + mỗi brand 1 BRAND currency `<BRAND>_PT`) + `cdp.point_conversion` (brand→group 1:1 seed).
  - `loyalty_entry.currency_id` (+ backfill entry cũ về group) + `loyalty_txn` thêm brand_id/store_id/source/ref_message_id/correlation_id.
  - Trigger `assert_loyalty_balanced` → cân **per (txn, currency_id)**.
  - `loyalty.service.ts`: postEntries/accountBalanceTx/balanceTx currency-aware; earn/reserve/settle/getBalance/listLedger/listMembers dùng GROUP mặc định; `getGroupCurrencyId` cache + `_resetLoyaltyCurrencyCache`.
  - `identity.repo transferLoyalty`: merge chuyển điểm **theo từng currency** (đa ví). Test `loyalty-coalition.spec.ts` (schema+seed+regression+trigger per-currency).
- [x] **L1 — Kernel đa-currency public HOÀN TẤT** (commit `c4e0459`, **605 test**): `convert` (đổi điểm brand→group/giữa currency qua point_conversion; burn from + mint floor(points*rate) to; cân per-currency; CURRENCY/CONVERSION_NOT_FOUND), `adjust` (± lý do bắt buộc + audit, RBAC data_steward, cấm âm), `transfer` (khách→khách, lock 2 occ, idempotent), `listWallets` (đa ví). API `v1/loyalty/{convert,adjust,transfer,wallets}` + schemas + LoyaltyError codes mới. Coverage loyalty.service ≥93%. FE rich để ở L9 (member-360).
- [x] **Checkpoint /review+/codex tài chính L0+L1 HOÀN TẤT** — sửa tận gốc các finding cross-model: (1) convert nhân điểm bằng SQL numeric `floor($::numeric*rate)` (bỏ float JS), resolve currency + tính toPoints TRƯỚC runOp, fingerprint theo **ID canonical**; (2) chặn toPoints=0/âm/vượt MAX; replay idempotent lấy đúng mint theo currency_id; (3) `resolveCurrencyId` phân biệt uuid vs code (UUID_RE), `is_active` bắt buộc; (4) unique index `uq_conv_open` chống nhiều tỷ giá mở cùng cặp; (5) `loyalty_entry.currency_id` **NOT NULL** sau backfill; (6) adjust/transfer fingerprint theo ID canonical, adjust gồm `reason` (replay khác lý do → CONFLICT); (7) `identity.transferLoyalty` giữ số dư dạng **string bigint** (bỏ `Number()` mất precision >2^53), âm/dương hoá trong SQL. tsc sạch, **605 test PASS**.
- [x] **L2 — Point lots + expiry/breakage HOÀN TẤT** (migration `026_loyalty_lots_expiry.sql`, **621 test**, checkpoint /review+/codex xong): `cdp.loyalty_lot` (occ, currency, points_original/remaining, earn_txn, issuing_company, expire_at, status active/exhausted/expired) + `cdp.expiration_policy` (per-currency, NONE/ROLLING/FIXED, seed ROLLING 24m). Bất biến **available(occ,cur) = Σ lô 'active'.remaining**: mọi thao tác TĂNG available → tạo lô; GIẢM → tiêu **FIFO theo expire_at**. Wire earn/reserve/release/capture/convert/adjust/transfer + merge (identity.repo). `expireLots` scheduler (`LoyaltyExpiryScheduler` 1h) → breakage `-available/+system:breakage`, idempotent per lô. **Checkpoint cross-model (Claude adversarial + codex) — sửa tận gốc:** (P0) migration **backfill lô** cho số dư available cũ (chống đóng băng điểm + sập merge/ingest); (P1) `consumeLotsFifoTx`/merge dùng **BigInt** (bỏ Number() mất precision >2^53); (P1) key đáo hạn namespaced `sys:expire:{lot}` + schema **cấm idempotencyKey prefix `sys:`**; (Medium) **BẢO TOÀN expire_at** qua reserve→release (cột `loyalty_reservation.consumed_lots` jsonb) + transfer + merge (chống "điểm bất tử" né breakage). Test: FIFO, expiry, breakage, reserved-không-hết-hạn, bảo toàn expiry, backfill, sys-reject. tsc sạch, zero regression.
- [x] **L3 — Earn rule engine + auto-earn HOÀN TẤT** (migration `027_loyalty_earn_rule.sql`, **633 test**, checkpoint /review+/codex xong): `cdp.earn_rule` (no-code: rate_per_unit × multiplier, scope brand/kênh, min_amount, conditions jsonb day_of_week/payment_method, qualifying, priority, window) **append-only + audit** (`earn_rule_audit` bất biến + trigger chặn UPDATE/DELETE **nội dung** earn_rule, chỉ đổi is_active); `earn-rule.service` (setEarnRule versioned, listRules, audit, `earnForTransaction`, `processUnearnedTransactions`); `postAutoEarnTx` (earn vào currency rule + lô L2 + qualifying, atomic). Auto-earn NGOÀI ingest qua `LoyaltyEarnScheduler` (30s) quét `canonical_transaction.loyalty_earned=false`. API `earn-rules` (data_steward) + `earn-run` (admin). **Checkpoint cross-model (Claude+codex) — sửa tận gốc:** (P1) earn + set cờ **atomic 1 transaction** + CLAIM `FOR UPDATE` + fingerprint **ổn định** `sys:autoearn:{msg}` (chống poison record/starvation khi rule đổi/đa-instance); (P1) **cap points ≤ MAX** (hóa đơn lớn không ném INVALID_AMOUNT gây kẹt); (P1) trigger **append-only earn_rule** ở DB; (P2) `day_of_week` theo **giờ VN** (`AT TIME ZONE Asia/Ho_Chi_Minh`); (P2) window kiểm theo **thời điểm giao dịch** (valid_from NULL=luôn hiệu lực); (P2) tie-break **min_amount DESC**. Ghi chú: L4 tier chỉ tính `type='earn' AND qualifying`. tsc sạch, zero regression.
- [ ] L3 (cũ) — thay bằng mục trên.
- [x] **L4 — Tier engine HOÀN TẤT** (migration `028_loyalty_tier.sql`, **640 test**, checkpoint /review+/codex xong): `tier_group`/`tier`/`member_tier` + `member_tier_history` (append-only). Qualify SPEND/VISITS/POINTS (NIGHTS hoãn PMS L8) trong cửa sổ ROLLING/CALENDAR; **lên hạng tức thì**, **xuống hạng soft-landing** (chỉ tại review_at + dưới ngưỡng giữ hạng=retention). `recomputeMemberTier`/`recomputeAllTiers` + `LoyaltyTierScheduler` (1h). Benefit `earn_multiplier` **tích hợp auto-earn** (L3↔L4). API `tier-groups`/`tiers`/`tiers/recompute[-all]`. Seed OCC_TIER 4 hạng (Member/Silver/Gold/Platinum theo chi tiêu 12m). **Checkpoint cross-model (Claude+codex) — sửa tận gốc:** (P1) `recomputeAllTiers` **chống starvation** (drain due-review có ORDER + keyset active theo cửa sổ, bỏ LIMIT-không-ORDER); (P1) CALENDAR review_at = đầu năm kế; (P1) recompute lock **`loyalty:occ`** khử race với auto-earn (điểm tất định); (P2) `getMemberEarnMultiplier` **defensive** (jsonb_typeof=number, JSON xấu→fallback 1); (P2) so review_at theo **now() DB** (chống lệch clock); (P2) parameterize interval; (P2) guard tier_id gỡ khỏi cấu hình. tsc sạch, zero regression.
- [x] **L5 — Reward catalog + redemption cross-brand HOÀN TẤT** (migration `029_loyalty_reward.sql`, **650 test**, checkpoint /review+/codex xong): `reward_catalog_item` (VOUCHER/GIFT/PAY_WITH_POINTS/PARTNER, cost điểm, tier_min, stock, validity) + `voucher` (FIXED/PERCENT/PRODUCT, expire, partial) + `voucher_redemption` (append-only). `redeemReward` (burn điểm **atomic** available→redeemed + lô L2 + phát voucher, idempotent), `useVoucher` (dùng tại brand, **FIXED tiêu partial**), `listRewards`/`listMemberVouchers`, `expireVouchers` (wire vào expiry scheduler). **Cross-brand**: cost điểm GROUP OCC_POINT, voucher redeemable_at_brand NULL=mọi brand. API `rewards`/`rewards/redeem`/`vouchers`/`vouchers/use`. **Checkpoint cross-model (Claude+codex) — sửa tận gốc:** (P1) `useVoucher` idempotency **scope theo voucher_id** (key trùng voucher khác→CONFLICT, chống double-benefit); (P1) `redeemReward` **replay TRƯỚC khi validate reward** (retry sau khi reward tắt/đổi vẫn trả voucher cũ, không burn kép); (P1) **precision**: cost_points string bigint + remaining_value trừ trong **SQL numeric** (bỏ float JS); (P2) validate brand tồn tại; (P2) expiry theo **now() DB** (bỏ Date.now/throw-after-commit); (P2) CHECK catalog VOUCHER phải có value>0. tsc sạch, zero regression.
- [ ] L6 — Campaign + gamification (promotion, referral, birthday, challenge/streak/lì xì) + FE.
- [ ] L7 — Liability + inter-company settlement (point_price, settlement_txn credit-in-arrears, liability_snapshot IFRS15/ASC606 theo pháp nhân, netting) + FE dashboard.
- [ ] L8 — POS/PMS realtime authorization (reserve→capture đồng bộ qua connector) + member_card (thẻ/QR) + stored-value wallet (nạp tiền/pay-with-points).
- [ ] L9 — Admin FE hoàn chỉnh workspace Loyalty (Programs/Currency/Rules/Tiers/Rewards/Campaigns/Member-360/Liability) + RBAC role loyalty_manager/loyalty_ops theo company/brand + báo cáo.

## Lưu ý kỹ thuật
- Migration ở **repo-root `db/migrations/`** (không phải services/core-api/db). Chạy tự động theo tên (migrate.ts), idempotent (IF NOT EXISTS/ON CONFLICT).
- truncateAll GIỮ company/point_currency/point_conversion (reference seed) — không truncate. Cache group currency id ổn định.
- Deploy: cần env `CONNECTOR_SECRET_KEY` (đã set). Migration 025 tự chạy lúc core-api start. Deploy mốc sau L2, L5, L9.
- Test: `pnpm exec vitest run` (592). Coverage patch ≥90% mỗi phase. Checkpoint /review→/codex ranh giới phase.
