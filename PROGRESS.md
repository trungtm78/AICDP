# PROGRESS — Tính năng "Kết nối" chạy THẬT (connector framework)

> File duy trì ngữ cảnh qua phiên. Sau `/clear`: đọc file này + plan gốc + `CLAUDE.md` TRƯỚC.
> Plan gốc: `~/.claude/plans/h-y-ph-n-t-ch-research-delightful-spindle.md` (đã research + threat-model đầy đủ).
> Lịch sử trước (Journey Builder, AI Phase A, rebrand, R0-R3d…) đã hoàn tất — xem git log + memory `MEMORY.md`.
> Branch `feat/foundation-core` · DB `AI_CDP_Pro` PG18 @5433 · demo live 192.168.1.93:8070 (admin/Och@2026).

## Quyết định kiến trúc (chốt)
- 1 engine adapter + auth-strategy registry + preset per-connector (phủ REST/OData/OAuth2); transport riêng RPC(Odoo)/SOAP/IPN(payment)/provider(GA4,Zalo…).
- ~40+ connector "Đã setup" (Group 1) / ~14 "Chưa setup" (Group 2, stub honest, không giả success).
- RudderStack: KHÔNG dùng rudder-server (ELv2 + phụ thuộc control-plane-cloud); TUỲ CHỌN nhúng rudder-transformer (MIT) cho 200+ destination.
- Bảo mật P0 (chặn go-live): SSRF guard · secret AES-256-GCM at-rest · webhook token constant-time · IPN checksum · consent gate egress · reverse-ETL chỉ-SELECT.
- **Checkpoint /review+/codex ở ranh giới PHASE** (framework là 1 khối cohesive; không mỗi micro-task). Commit + coverage mỗi task.
- Coverage: đã cài `@vitest/coverage-v8@3.2.6` (devDep). Đo: `pnpm exec vitest run <files> --coverage --coverage.provider=v8 --coverage.include='...' --coverage.reporter=json-summary --coverage.reportsDirectory=./cov-tmp` rồi đọc `cov-tmp/coverage-summary.json`.

## Phase (milestone)
- [x] **Phase 1 — Framework + nền bảo mật P0 HOÀN TẤT** (7 commit 6f79aba→30d1077). Qua checkpoint /review (tìm P0 SSRF IPv6-mapped + 5 hardening) + /codex cross-model (tìm 2×P1 rò secret plaintext + SSRF fail-closed) — TẤT CẢ đã fix. **418 test PASS**, tsc sạch, patch coverage ≥90% mọi file.
- [~] **Phase 2 — INBOUND thật (ĐANG LÀM)**:
  - [x] **Task 2.1 — cổng webhook** (commit `2b99bd7`): `@Public() POST /v1/connectors/sources/:id/events` (`src/http/connector-ingest.controller.ts`) header X-Connector-Token → `resolveInboundConnection` → `ingestInboundEvent` (`src/connector/inbound-ingest.service.ts`: map brand_id TỪ connection, zod strict, INVALID_IDENTIFIER guard, ghi connector_event ingested/rejected) → `ingestOrderCompleted`/`ingestIdentify`. Rate-limit per-connection `connector-inbound-ratelimit.ts` (TokenBucket, luôn bật). ErrorCode mới: CONNECTOR_MISCONFIGURED, CONNECTOR_RATE_LIMIT. 11 e2e + 2 unit, coverage 100% line 3 file mới. **431 test PASS.**
  - [x] **Task 2.2 — write-key (shape Segment)** (commit `2cc64c3`): `@Public() POST /v1/connectors/track` auth writeKey (Basic chuẩn Segment | X-Write-Key) → `resolveWriteKeyConnection` (tra config.writeKey, 401 đồng nhất) → `mapSegmentPayload` (`src/connector/segment-map.ts`, thuần: track 'Order Completed'/orderEvent tuỳ biến + identify → shape inbound; track khác → UNKNOWN) → ingestInboundEvent. `extractWriteKey` helper. 7 e2e + 10 unit, coverage ≥97%. **448 test PASS.**
  - [x] **Task 2.3 — Postgres reverse-ETL puller** (commit `009f55a`): `adapters/inbound-postgres.ts` (InboundPullAdapter src_pg): **SELECT-only** (dựng câu lệnh từ identifier ĐÃ validate regex+quote, KHÔNG SQL thô; `BEGIN TRANSACTION READ ONLY` + statement_timeout; cursor tham số hoá) + **host-guard SSRF** (chặn IP nội bộ trừ `CONNECTOR_ALLOW_PRIVATE_PULL=1`). `inbound-pull.service.pullConnection` (đọc/tiến pull_cursor, decrypt config, đếm ingested/rejected) + `POST /connections/:id/pull` (data_steward). `adapters/register-all.ts` đăng ký (import ở app.factory → src_pg 'ready'). 6 e2e + 5 unit, coverage ≥97%. **459 test PASS.**
  - ✅ **PHASE 2 CODE HOÀN TẤT** (3 commit `2b99bd7`→`009f55a`): INBOUND thật = webhook token + write-key Segment + reverse-ETL PG. Adapter framework outbound vẫn chờ Phase 4.
  - ✅ **CHECKPOINT /review+/codex XONG** (commit `e694c28`): Claude adversarial subagent + codex 0.139 cross-model. Vá P0/P1/P2: **DNS-rebinding pin IP + TLS** (resolvePinnedHost), **cursor no-skip stop-at-failure + per-event cursor** (chống mất dữ liệu row rejected), **cursor tie-breaker composite** (chống skip khi cursorColumn trùng), **advisory-lock concurrent pull (409)**, **pull status=active**, **allowInternalHost per-connection**, **timing-oracle dummy-compare**, **writeKey uniform 401 + collision reject**, **total NULL→rejected**, **inbound identifier .strict()**. +12 test. **471 test PASS**, coverage ≥99% line. Deferred (ghi chú, không blocker): store_id∈brand policy, total bigint>2^53 (money-type toàn hệ thống), flood pre-DB (IpRateLimitGuard đã có).
  - ⚠️ **FLAKE cần điều tra**: full suite 1 lần hiếm gặp fast-check fail trong `src/expert/` (property identity/loyalty, seed -1205792704 path "4"); chạy lại + 4× expert isolated đều PASS 33/33. Pre-existing, KHÔNG do Phase 2. Ghi lại để soi property test nào (endOnFailure random seed).
- [x] **Phase 2 — INBOUND thật HOÀN TẤT + hardened** (4 commit `2b99bd7`→`e694c28`, **471 test PASS**). Qua checkpoint /review (Claude adversarial) + /codex cross-model — vá 10 finding P0/P1/P2.
- [x] **Phase 3 — Cổng thanh toán IPN HOÀN TẤT + hardened** (3 commit `0eff46a`→`a54a415`, **509 test PASS**). VNPay (SHA512) + MoMo/ZaloPay (SHA256) IPN: `@Public POST/GET /v1/connectors/payment/:id/ipn`. `connector/payment/{vnpay,momo,zalopay,payment-ipn.service}.ts`. Verify checksum fail-closed + **BIND merchantId đã ký (vnp_TmnCode/partnerCode/app_id) chống replay cross-connection** + brand/store từ connection + chống replay (message_id idempotent) + **txnRef/store_id cấm ':'** (namespace integrity) + amount isSafeInteger+cap + rate-limit post-verify. Catalog thêm src_vnpay/src_momo/src_zalopay (secretKey secret + merchantId/accessKey). Qua checkpoint /review (Claude: P0 checksum ĐẠT) + /codex (P0 cross-connection replay → đã vá). Deferred: message_id composite-key an toàn thay ':' join (đã chặn ':' ở input nên đủ); ZaloPay success=ok (protocol-correct).
- [x] **Phase 4 — OUTBOUND thật + wiring activation HOÀN TẤT + hardened** (4 commit `2ee1ae0`→`c7cc7e4`, **552 test PASS**). OutboundAdapter mở rộng deps (tiêm fetch test). Adapter: `dst_webhook` (safeFetch + ký HMAC X-OCC-Signature), `dst_ga4` (Measurement Protocol), `dst_zalo_zns` (message/template). `outbound.service.deliverToConnection` (ghi connector_delivery, redact IP/URL error, không sập cả lô) + `deliverActivationRun` (re-check consent latest-wins + idempotency chống double-send). Endpoints `POST /connections/:id/test-send` + `POST /v1/activation/:runId/deliver`. safeFetch giới hạn body 2MB. recipient PII mask; webhook authHeader/signingSecret khai secret. Qua /review (Claude) + /codex — vá P1 consent-window + P1 idempotency + P1 secret-plaintext + P2 (rò IP/body-DoS/PII/recipient-URL). Files: `connector/adapters/{outbound-webhook,outbound-ga4,outbound-zalo-zns,outbound-util}.ts` + `connector/outbound.service.ts`.
- [x] **Phase 5 — VN 1B + messaging 1B HOÀN TẤT + hardened** (3 commit `ef564e5`→`887963d`, **571 test PASS**). OUTBOUND SMS: `dst_esms`(eSMS.vn SendMultipleMessage_V4) + `dst_vietguys`(cloudsms form). INBOUND VN qua **mapper cấu hình field-path** (`payload-map.applyPayloadMapping`): mọi nguồn (KiotViet/GHN/MISA/custom) POST native + `config.payloadMapping` (dot-path own-prop guard + hằng '=') → order_completed/identify; wire vào `/events`. Catalog thêm src_kiotviet/src_ghn/src_misa + dst_esms. Qua /review (Claude+codex, proto-pollution AN TOÀN) — vá P2: getPath hasOwnProperty+cap, num() cấm "1.500" sai tiền, SMS default không lộ audience, pos_transaction_id cấm ':'. Files: `connector/adapters/{outbound-esms,outbound-vietguys}.ts` + `connector/payload-map.ts`.
- [x] **Phase 6 — ERP REST/OData HOÀN TẤT + hardened** (2 commit `829e962`+`b0a300d`, **585 test**). `adapters/inbound-rest` (src_rest/src_sap/src_oracle): GET phân trang safeFetch → recordsPath → payloadMapping → ingest, tái dùng pullConnection. Qua /review (Claude+codex, KHÔNG P0/P1 dư) — vá: nextLink cùng-origin CẢ 2 chiều (chống rò Bearer + poison cursor) + relative resolve, cap record/trang ≤500 (DoS), authHeaderName validate, mapping-array, preset SAP(d.results/d.__next)/Oracle(items), getField key-literal.
- [x] **Phase 7 BACKEND — install-status honest 2-group** (commit `9ecb807`, **586 test**). `getCatalog` gắn `installStatus` ('ready' đã setup / 'planned' chưa setup) theo hiện thực THẬT. Group 2 đã honest sẵn (INTEGRATION_NOT_AVAILABLE, không giả success).
- [ ] **Phase 7 FRONTEND + DEPLOY (RESUME TỪ ĐÂY — phần còn lại DUY NHẤT)**: **CONNECTOR BACKEND ĐÃ XONG TOÀN BỘ** (Phase 1-6 + 7-backend, 24 commit `6f79aba`→`9ecb807`, 586 test, mọi phase qua /review+/codex). Còn lại:
  1. **FE admin-console (React) workspace "Kết nối"** (`apps/admin-console/src`): (a) catalog chia **2 nhóm theo `installStatus`** ('ready'=Đã setup / 'planned'=Chưa setup — dùng field mới từ `GET /v1/connectors/catalog`); (b) sau khi kết nối, **hiện LOẠI DATA** đang chảy qua `GET /v1/connections/:id/data-summary` (đã có: events theo type + deliveries theo status → chip "doanh thu/điểm/thanh toán/tin gửi/lỗi"); (c) nút **Test-send** (`POST /connections/:id/test-send`), **Pull** (`POST /connections/:id/pull`), **cấp inbound-token** (`POST /connections/:id/inbound-token` reveal 1 lần), xem **events/deliveries** (`GET .../events|/deliveries`); (d) form config theo `configFields` (đã có, field secret hiện password). Endpoint FE cần: catalog/connections CRUD (đã có), test-send/pull/inbound-token/events/deliveries/data-summary (đã có). Tham chiếu màn Kết nối cũ `apps/admin-console/src` (workspace `/connectors` đã có từ trước — MỞ RỘNG thêm 2-group + data-summary + nút vận hành).
  2. **Deploy VM** 192.168.1.93:8070 (ssh aicdp, docker compose `deploy/docker-compose.prod.yml`, sudo `Odood3mo2026`): build FE → `deploy/admin-dist` → tar `services/core-api db deploy ...` → scp → `sudo docker compose up -d --build core-api web` → **restart web** nếu recreate core-api (nginx cache DNS) → re-seed exec-in-container `-e SEED_ADMIN_PASSWORD=Och@2026`. Migration 024 tự chạy lúc core-api start. **LƯU Ý: mạng TRUNG-PC→VM nghẽn download.**
  3. Checkpoint FE: /qa Playwright (E2E thật click qua UI, cấm seed API) + verify live 8070.
- [ ] Phase 4 — OUTBOUND 1A (analytics+messaging verify-free) + wiring activation
- [ ] Phase 5 — VN 1B (KiotViet/GHN/MISA…) + messaging 1B (Zalo ZNS/VietGuys/eSMS…)
- [ ] Phase 6 — ERP (generic REST/OData + preset) + warehouse
- [ ] Phase 7 — Group 2 stub + Frontend 2-group + deploy

## Phase 1 — task
- [x] **Task 1 — SSRF guard** `src/connector/ssrf-guard.ts`(+spec): `assertSafeUrl` (https-only, chặn IP nội bộ/metadata/CGNAT, resolve-then-pin chống DNS-rebinding, no userinfo) + `isPrivateIp` (v4/v6/mapped). 15 test PASS, line **92.23%**.
- [x] **Task 2 — Secret encryption** `src/connector/secrets.ts`(+spec): AES-256-GCM encrypt/decrypt/mask + encrypt/decrypt/maskConfig + `getConnectorSecretKey` (fail-fast prod). 14 test PASS, line **97.5%**. ErrorCode connector thêm ở `src/http/errors.ts`.
- [x] **Task 3 — Migration 024** `db/migrations/024_connector_live.sql`: connection cols (inbound_token_hash, oauth_state, last_checked_at, last_error, pull_cursor) + `connector_event` + `connector_delivery` (FK cascade). Thêm vào `test-helpers/db.ts` truncateAll. Spec `connector-schema.spec.ts` 4 test (chạy migration thật PG18 + CHECK + cascade).
- [x] **Task 4 — Framework core**: `signing.ts` (hmacHex/verifyHmac/timingSafeEqualStr — 100%) + `adapters/types.ts` (interfaces) + `adapters/registry.ts` (register/get/installStatus — 100%) + `http-client.ts` (safeFetch: assertSafeUrl + pin IP undici Agent + timeout + redirect-manual + CRLF strip; 93% line). Dep thêm: `undici` (types + Agent). 21 test.
- [x] **Task 5a — VÁ lỗ hổng plaintext (P0)**: `connector.service.ts` — `createConnection` encryptConfig(secret fields) trước INSERT; `mapConnection` maskConfig (API luôn mask, không lộ plaintext/ciphertext); thêm `secretFieldsFor` (catalog ∪ custom config_schema) + `getConnectionConfigDecrypted` (nội bộ, giải mã). Spec `connector-secrets.spec.ts` 5 test (real DB): mask trả API, DB lưu ciphertext, decrypt nội bộ, secretFieldsFor catalog+custom.
- [x] **Task 5b — services nền + endpoints** (XONG): `logs.service.ts` (recordEvent/listEvents/recordDelivery/listDeliveries — 100%), `data-summary.service.ts` (dataSummary đếm event/delivery — 100%), `inbound.service.ts` (issueInboundToken reveal-1-lần + resolveInboundConnection constant-time — 100%), `health.service.ts` (testConnection real qua adapter, set status/last_checked/last_error, INTEGRATION_NOT_AVAILABLE nếu chưa có adapter — 100%). Endpoints connector.controller: POST /connections/:id/test, /inbound-token, GET /connections/:id/{events,deliveries,data-summary}. E2e `connector.e2e.spec.ts` 5 test (mask, token, data-summary, honest-stub 400, 401). **Phase 1 CODE HOÀN TẤT.**
- [~] **Chi tiết Task 5b (đã làm):** 
  - `delivery.service.ts`: `recordDelivery(pool, {...})` INSERT connector_delivery; `listDeliveries(pool, connectionId)`; `recordEvent(pool,{...})` INSERT connector_event; `listEvents(pool, connectionId)`. (redrive/deliverRun THẬT để Phase 4.)
  - `data-summary.service.ts`: `dataSummary(pool, connectionId)` đếm connector_event theo status/event_type + connector_delivery theo status → map sang chip data-entity (dùng `dataEntities` — SẼ thêm vào catalog metadata ở Phase 7/khi cần). Phase 1 chỉ cần đếm event/delivery.
  - `health.service.ts`: `testConnection(pool, id)` — lấy getConnectionConfigDecrypted + registry.getOutbound(connectorKey); có adapter → gọi healthCheck, set last_checked_at/last_error/status ('active'|'error'); không adapter → INTEGRATION_NOT_AVAILABLE. TDD với fake adapter đăng ký registry.
  - `inbound.service.ts`: `issueInboundToken(pool,id)` randomBytes(24) reveal raw 1 lần + lưu sha256 vào inbound_token_hash; `resolveInboundConnection(pool, id, token)` timingSafeEqualStr hash, status active, direction source.
  - Endpoints trong `connector.controller.ts` (đã @Roles data_steward): `POST /connections/:id/test` (health), `GET /connections/:id/events`, `GET /connections/:id/deliveries`, `GET /connections/:id/data-summary`, `POST /connections/:id/inbound-token` (reveal 1 lần). Zod schema mới trong `http/schemas.ts` nếu cần body.
  - Lưu ý: các service nhận `pool` tham số (không DI) như connector.service hiện tại; controller inject PG_POOL sẵn.
- [ ] Checkpoint Phase 1: verification-before-completion → /review → /codex (SAU review).

## Test/coverage
- Full BE suite: **394 PASS / 0 FAIL** · tsc BE sạch. Coverage patch mỗi file ≥90% line (ssrf 92%, secrets 97.5%, signing 100%, registry 100%, http-client 93%, connector-secrets qua real-DB spec).

## Commit Phase 1 (7)
- `6f79aba` Task 1-2 (ssrf-guard + secrets + ErrorCode + coverage-v8)
- `b36cabb` Task 3-4 (migration 024 + signing/registry/http-client + undici)
- `3184184` Task 5a (connector.service encrypt/mask, vá plaintext)
- `a5f2ac0` Task 5b (logs/data-summary/inbound/health + endpoints)
- `b1f81d0` /review fixes (P0 SSRF IPv6-mapped hex + 5 hardening)
- `30d1077` /codex fixes (2×P1 rò secret + SSRF fail-closed ::ffff:0:0/96, fec0::/10)

## Adapter framework — API sẵn cho Phase 2+
- `adapters/registry.ts`: registerOutbound/getOutbound/installStatus; `registerInboundPull/getInboundPull`.
- `adapters/types.ts`: OutboundAdapter{deliver,healthCheck} · InboundPullAdapter{pull} · DeliveryResult · HealthResult.
- `http-client.ts safeFetch`: gọi HTTP ngoài AN TOÀN (SSRF + pin IP) — adapter outbound PHẢI dùng cái này.
- `signing.ts`: hmacHex/verifyHmac/timingSafeEqualStr (IPN + webhook signature).
- `secrets.ts`: getConnectionConfigDecrypted (connector.service) cho adapter đọc config đã giải mã.
- `logs.service.ts recordEvent/recordDelivery` · `data-summary.service dataSummary` · `inbound.service resolveInboundConnection`.

## Nợ/lưu ý
- Lỗ hổng hiện trạng: `cdp.connection.config` lưu plaintext + trả nguyên qua `GET /connections` (`connector.service.ts:100`, `connector.controller.ts:66`) → Task 5/FE vá bằng encryptConfig lúc tạo + maskConfig lúc đọc.
- Môi trường: core-api tsx KHÔNG emit decorator metadata → DI @Inject tường minh (verify boot thật); seed lại sau khi test truncate DB.
